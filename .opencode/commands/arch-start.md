---
description: Start or resume an Archsmith API session
agent: archsmith-build
---
Use the `archsmith_start_session` tool and initialize a session for architecture work.
If a `session_id` is already present in the conversation, reuse it.
Return the effective `session_id`, `icon_set`, and readiness checks.
